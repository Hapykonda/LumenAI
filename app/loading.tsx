import { LumenLoadingScreen } from "@/components/ui/lumen-loading-screen";

export default function Loading() {
  return <LumenLoadingScreen title="Preparando LumenAI" message="Cargando la experiencia principal." />;
}
